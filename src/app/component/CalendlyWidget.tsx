'use client'; // Required for the app directory

import { InlineWidget } from "react-calendly";

const CalendlyWidget = () => {
  return (
    <InlineWidget url="https://calendly.com/larrylo1107/30min" />
  );
};

export default CalendlyWidget;